# user configs (copy from config.example.zon; contain real subscription tokens, never commit)
config.zon

# install-time backups (deploy renames old configs to .bak)
*.bak

# Zig build artifacts
.zig-cache/
zig-out/

# test client binaries (downloaded from GitHub releases)
bin/

# collaborative planning docs (working files, not project artifacts)
plan.md
notes.md

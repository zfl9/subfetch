# user config (copy from subscriptions.example.zon; contains real subscription tokens, never commit)
subscriptions.zon

# test artifacts
test-output.yaml
test-output.yaml.bak
secret.txt
*.bak

# Zig
.zig-cache/
zig-out/

# test client binaries (downloaded from GitHub releases)
bin/

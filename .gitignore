# user config (copy from config.example.zon; contains real subscription tokens, never commit)
config.zon

# test artifacts
*.bak

# Zig
.zig-cache/
zig-out/

# test client binaries (downloaded from GitHub releases)
bin/
real.zon

# collaborative planning docs (working files, not project artifacts)
plan.md
NOTES.md
todo.md
